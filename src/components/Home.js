import React from "react";
import Balance from "./Balance";
import Add from "./transaction/Add";

const Home = () => {
  return (
    <div>
      <Balance />
      <Add></Add>
    </div>
  );
};

export default Home;
